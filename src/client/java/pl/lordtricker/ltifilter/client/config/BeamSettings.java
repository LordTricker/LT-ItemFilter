package pl.lordtricker.ltifilter.client.config;

public class BeamSettings {
    // Kolor zapisany w formacie hex (np. "#80ccff")
    public String hexColor = "#80ccff";
    // Przezroczystość beama
    public float alpha = 1.0f;
    // Wysokość słupa beama
    public float height = 1.0f;
    // Promień beama
    public float radius = 0.05f;
    // Przesunięcie pionowe beama od ziemi (np. 0.5 - beam zacznie się 0.5 bloku nad ziemią)
    public float verticalOffset = 0.0f;
}
